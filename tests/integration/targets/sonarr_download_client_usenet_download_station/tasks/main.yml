---
- name: create download client
  devopsarr.sonarr.sonarr_download_client_usenet_download_station:
    remove_completed_downloads: false
    remove_failed_downloads: false
    enable: false
    priority: 1
    name: "UsenetDownloadStation"
    host: "localhost"
    port: 5000
    use_ssl: false
    tv_category: "tv-sonarr"
    username: "user"
    password: "password"
    sonarr_api_key: "{{ sonarr_api_key }}"
    sonarr_url: "{{ sonarr_url }}"
  register: result
- name: assert download client creation
  assert:
    that:
      - result['implementation'] == 'UsenetDownloadStation'
# ----------------------------------------------------------------------------
- name: edit already present download client
  devopsarr.sonarr.sonarr_download_client_usenet_download_station:
    remove_completed_downloads: false
    remove_failed_downloads: false
    enable: false
    priority: 1
    name: "UsenetDownloadStation"
    host: "localhost"
    port: 5000
    use_ssl: false
    tv_category: "tv-sonarr"
    username: "userName"
    password: "password"
    sonarr_api_key: "{{ sonarr_api_key }}"
    sonarr_url: "{{ sonarr_url }}"
  register: result
- name: assert download client changes
  assert:
    that:
      - result.changed == true
# ----------------------------------------------------------------------------
- name: test idempotency
  devopsarr.sonarr.sonarr_download_client_usenet_download_station:
    remove_completed_downloads: false
    remove_failed_downloads: false
    enable: false
    priority: 1
    name: "UsenetDownloadStation"
    host: "localhost"
    port: 5000
    use_ssl: false
    tv_category: "tv-sonarr"
    username: "userName"
    password: "password"
    sonarr_api_key: "{{ sonarr_api_key }}"
    sonarr_url: "{{ sonarr_url }}"
  register: result
- name: assert download client idempotency
  assert:
    that:
      - result.changed == false
# ----------------------------------------------------------------------------
- name: delete download client
  devopsarr.sonarr.sonarr_download_client_usenet_download_station:
    name: "UsenetDownloadStation"
    sonarr_api_key: "{{ sonarr_api_key }}"
    sonarr_url: "{{ sonarr_url }}"
    state: 'absent'
  register: result
- name: assert download client deletion
  assert:
    that:
      - result['id'] == 0